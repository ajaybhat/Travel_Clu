A web app for a travel club, to book trips.

<p>
Uses a MySQL DB with login credentials:
	<br>username:airwatch
	<br>password:airwatch
</p>
<p>
Build using Maven. cd to the project directory and type in the command line:
<br/>
	<code>
		mvn package.
	</code>
</p>